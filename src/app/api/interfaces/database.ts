export enum DbTable {
  ACTIVITY = "activities",
  COLLECTIBLE = "collectibles",
  EVENT = "events",
  PR = "pr",
  STAT = "stats",
  USER = "users",
  USER_BADGE = "userBadges",
}

export enum DbError {
  INSERT = "Error inserting data",
  UPDATE = "Error updating data",
}
