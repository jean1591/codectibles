export enum DbTable {
  ACTIVITY = "activities",
  BADGE = "badges",
  COLLECTIBLE = "collectibles",
  EVENT = "events",
  PR = "pr",
  USER = "users",
}

export enum DbError {
  INSERT = "Error inserting data",
  UPDATE = "Error updating data",
}
