export enum DbTable {
  ACTIVITY = "activity",
  BADGE = "badge",
  COLLECTIBLE = "collectible",
  EVENT = "event",
  PR = "pr",
  USER = "user",
}

export enum DbError {
  INSERT = "Error inserting data",
  UPDATE = "Error updating data",
}
